# 情报看板数据后端 — 部署指南

## 整体架构

```
外部网站
  ↓ (GitHub Actions 定时爬取)
爬虫脚本 (Python)
  ↓ (飞书 API 写入)
飞书多维表格 (数据中转层)
  ↓ (nocode 读取)
情报看板前端
```

---

## 第一步：创建飞书应用

1. 打开 [飞书开放平台](https://open.feishu.cn/app)，点击「创建企业自建应用」
2. 应用名称：`情报看板数据服务`
3. 进入应用后，找到「凭证与基础信息」，记录：
   - `App ID`
   - `App Secret`
4. 在「权限管理」中开启以下权限：
   - `bitable:app`（多维表格读写）
   - `bitable:app:readonly`
5. 发布应用（版本管理 → 创建版本 → 申请发布）

---

## 第二步：创建飞书多维表格

在飞书中新建一个多维表格，命名为「情报看板数据库」，创建以下 **5 张表**：

### 表1：行业快讯
| 字段名 | 类型 | 说明 |
|--------|------|------|
| 标题 | 文本 | 文章标题 |
| 摘要 | 文本 | 内容摘要（≤500字） |
| 来源 | 单选 | 36氪/虎嗅/TechCrunch 等 |
| 地区 | 单选 | 国内/海外 |
| 发布时间 | 文本 | 原始发布时间 |
| 链接 | 超链接 | 原文链接 |
| 唯一ID | 文本 | URL的MD5，用于去重 |
| 抓取时间 | 文本 | 爬虫抓取时间 |

### 表2：咨询报告
| 字段名 | 类型 | 说明 |
|--------|------|------|
| 报告标题 | 文本 | 报告名称 |
| 机构 | 单选 | McKinsey/BCG 等 |
| 分类 | 单选 | 战略咨询/人力资源/科技研究/职场趋势 |
| 摘要 | 文本 | 报告摘要 |
| 发布日期 | 文本 | 发布日期 |
| 报告链接 | 超链接 | 原文链接 |
| 唯一ID | 文本 | 去重用 |
| 抓取时间 | 文本 | 爬虫抓取时间 |

### 表3：宏观事件待筛选（每周一自动填入，人工审核）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| 标题 | 文本 | 新闻标题 |
| 摘要 | 文本 | 内容摘要 |
| 来源 | 单选 | 新华社/人民日报等 |
| 发布日期 | 文本 | 发布日期 |
| 链接 | 超链接 | 原文链接 |
| 状态 | 单选 | **待审核** / 已采纳 / 已忽略 |
| 唯一ID | 文本 | 去重用 |
| 抓取时间 | 文本 | 爬虫抓取时间 |

> ⚠️ 每周一收到通知后，进入此表，将需要展示的条目状态改为「已采纳」，nocode 看板只读取「已采纳」的记录。

### 表4：关键宏观事件（人工整理后的正式展示表）
| 字段名 | 类型 | 说明 |
|--------|------|------|
| 标题 | 文本 | 事件标题 |
| 摘要 | 文本 | 简要摘要（≤200字） |
| 来源 | 文本 | 信息来源 |
| 发布日期 | 文本 | 事件日期 |
| 链接 | 超链接 | 原文链接 |
| 重要程度 | 单选 | 高/中/低 |
| 更新时间 | 文本 | 最后更新时间 |

> 💡 此表由你手动维护：从「宏观事件待筛选」中选出重要事件，复制到此表并补充摘要。

### 表5：月度宏观指标
| 字段名 | 类型 | 说明 |
|--------|------|------|
| 指标名称 | 单选 | GDP增速/CPI同比等 |
| 年月 | 文本 | 格式：2024-03 |
| 数值 | 数字 | 指标数值 |
| 单位 | 文本 | %或空 |
| 同比变化 | 文本 | 与去年同期差值 |
| 环比变化 | 文本 | 与上月差值 |
| 数据来源 | 文本 | 国家统计局/Excel导入 |
| 更新时间 | 文本 | 最后更新时间 |
| 唯一ID | 文本 | 指标名称_年月，去重用 |

---

## 第三步：获取表格 ID

1. 打开飞书多维表格，URL 格式为：
   `https://xxx.feishu.cn/base/xxxxxxxx?table=tblxxxxxxxx`
2. URL 中 `/base/` 后面的部分是 **App Token**（如 `xxxxxxxx`）
3. `?table=` 后面的部分是 **Table ID**（如 `tblxxxxxxxx`）
4. 分别记录 5 张表的 Table ID

---

## 第四步：将应用添加到多维表格

1. 打开多维表格 → 右上角「...」→「更多」→「添加文档应用」
2. 搜索并添加你创建的「情报看板数据服务」应用
3. 授予「可编辑」权限

---

## 第五步：配置 GitHub 仓库

1. 将 `intelligence-dashboard` 文件夹上传到 GitHub 新建仓库
2. 进入仓库 → Settings → Secrets and variables → Actions
3. 添加以下 Secrets：

| Secret 名称 | 值 |
|-------------|-----|
| `FEISHU_APP_ID` | 飞书应用的 App ID |
| `FEISHU_APP_SECRET` | 飞书应用的 App Secret |
| `FEISHU_APP_TOKEN` | 多维表格的 App Token |
| `TABLE_ID_NEWS` | 行业快讯表 ID |
| `TABLE_ID_REPORTS` | 咨询报告表 ID |
| `TABLE_ID_MACRO_PENDING` | 宏观事件待筛选表 ID |
| `TABLE_ID_MACRO_STATS` | 月度宏观指标表 ID |
| `FEISHU_WEBHOOK` | （可选）飞书机器人 Webhook，用于接收通知 |

---

## 第六步：导入 Excel 存量数据

```bash
# 本地运行（需要先设置环境变量）
cd crawlers
pip install -r requirements.txt

# 设置环境变量（Windows PowerShell）
$env:FEISHU_APP_ID="your_app_id"
$env:FEISHU_APP_SECRET="your_app_secret"
$env:FEISHU_APP_TOKEN="your_app_token"
$env:TABLE_ID_MACRO_STATS="tblxxxxxxxx"

# 导入宏观指标存量数据
python macro_stats_updater.py --import 宏观指标历史数据.xlsx
```

**Excel 格式要求（第一行为表头）：**
```
指标名称 | 年月     | 数值 | 单位 | 同比变化 | 环比变化
GDP增速  | 2024-01  | 5.2  | %    | +0.1     | -0.3
CPI同比  | 2024-01  | 0.7  | %    | -0.2     | +0.1
```

---

## 第七步：在 nocode 中配置数据源

在 nocode 看板中，为每个板块配置飞书多维表格数据源：

| 看板板块 | 对应飞书表 | 筛选条件 |
|----------|-----------|---------|
| 行业快讯侧边栏 | 行业快讯 | 按抓取时间倒序，取最新 50 条 |
| 宏观指标趋势图 | 月度宏观指标 | 按年月排序，取最近 12 个月 |
| 关键宏观事件卡片 | 关键宏观事件 | 按重要程度+日期排序 |
| 咨询报告卡片 | 咨询报告 | 按抓取时间倒序，取最新 20 条 |
| 社招趋势图 | 月度宏观指标（筛选社招相关指标） | 最近 12 个月 |

---

## 日常运维流程

### 每周一（宏观新闻审核）
1. 收到飞书机器人通知
2. 打开「宏观事件待筛选」表
3. 将重要事件状态改为「已采纳」
4. 将采纳的事件手动复制到「关键宏观事件」表，补充摘要

### 每月（宏观指标校准）
1. 收到飞书机器人通知（每月 15 日）
2. 打开「月度宏观指标」表，核对最新一期数据
3. 如有误差，手动修正

### 每月（社招趋势更新）
1. 准备好社招趋势 Excel 数据
2. 本地运行：`python macro_stats_updater.py --import 社招趋势.xlsx`
   （或在 GitHub Actions 手动触发 macro_stats 任务并上传文件）

### Top3 热点（每日）
- 直接在 nocode 看板界面手动编辑 Top3 内容和学城链接

---

## 文件结构

```
intelligence-dashboard/
├── .github/
│   └── workflows/
│       └── crawlers.yml          # GitHub Actions 定时任务配置
├── crawlers/
│   ├── feishu_client.py          # 飞书 API 封装
│   ├── news_crawler.py           # 行业快讯爬虫（17个信源）
│   ├── report_crawler.py         # 咨询报告爬虫（26个机构）
│   ├── macro_news_crawler.py     # 宏观新闻周度抓取
│   ├── macro_stats_updater.py    # 宏观指标更新（含Excel导入）
│   └── requirements.txt          # Python 依赖
└── SETUP_GUIDE.md                # 本文件
```
